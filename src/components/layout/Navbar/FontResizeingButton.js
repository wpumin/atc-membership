import React from 'react'

function FontResizingButton(props) {
    return (
        <button className={props.class} onClick={props.onClick}>
            A
        </button>
    )
}

export default FontResizingButton
